{
  "name": "CryptoBeat",
  "license": "UNLICENSED",
  "scripts": {
    "prepare:goerli": "mustache networks/goerli.json subgraph.template.yaml > subgraph.yaml",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ sodamnfoolish/CryptoBeat",
    "create-local": "graph create --node http://localhost:8020/ sodamnfoolish/CryptoBeat",
    "remove-local": "graph remove --node http://localhost:8020/ sodamnfoolish/CryptoBeat",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sodamnfoolish/CryptoBeat",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.1",
    "@graphprotocol/graph-ts": "0.27.0",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
